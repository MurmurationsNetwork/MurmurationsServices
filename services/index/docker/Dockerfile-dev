FROM golang:alpine

RUN apk update

WORKDIR /src/index

COPY go.mod go.sum ./
RUN go mod download

RUN go install github.com/cespare/reflex@latest

CMD ["reflex", "-c", "./services/index/reflex.conf"]
