package index

// IndexQueueGroup is the name of the NATS queue group for index operations.
// It is used to identify all subscribers that are interested in listening to index events.
const IndexQueueGroup = "index-qgroup"
